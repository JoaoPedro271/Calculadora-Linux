#ifndef Biblioteca_h
#define Biblioteca_h

int soma(float v1, float v2){
	
	float resultado;
	resultado = v1 + v2;
	return(resultado);
}
int subtracao(float v1, float v2){

	float resultado;
	resultado = v1 - v2;
	return(resultado);
}
int multiplicacao(float v1, float v2){

	float resultado;
	resultado = v1 * v2;
	return(resultado);
}
int divisao(float v1, float v2){

	float resultado;
	resultado = v1 / v2;
	return(resultado);
}

#endif
